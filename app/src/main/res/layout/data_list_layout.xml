<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#fffff0"
    android:orientation="vertical"
    android:theme="@style/Theme.MaterialComponents.Light">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/list_of_user_profiles_recycler"
        android:layout_width="match_parent"
        android:layout_marginTop="64dp"
        android:layout_height="match_parent"
        android:paddingTop="@dimen/list_item_spacing_half"
        android:paddingBottom="@dimen/list_item_spacing_half"
        tools:context="app.exploitr.login.platinum.ui.dialogues.UserListDialog"
        tools:listitem="@layout/isp_list_layout_item" />

    <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
        android:id="@+id/add_new_profile"
        android:layout_width="wrap_content"
        android:layout_height="48dp"
        android:layout_gravity="start"
        android:layout_margin="16dp"
        android:text="@string/add_profile"
        app:backgroundTint="@color/bl"
        app:icon="@drawable/ic_add"
        android:textColor="@color/wh"
        app:iconTint="@color/purp" />
</androidx.coordinatorlayout.widget.CoordinatorLayout>